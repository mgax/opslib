Tutorial
========

.. toctree::
   :caption: Contents:
   :maxdepth: 1

   layout

This tutorial will walk you through deploying Gitea using opslib. We will run
it locally at first, using docker-compose, set up backups, configure a Git
repository, then deploy it to a VPS, and expose it to the Internet using a
Cloudflare tunnel.

Continue to :doc:`layout`.
